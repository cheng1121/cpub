name: cpub
description: A sample command-line application.
version: 0.0.2
homepage: https://github.com/chengbook

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
#  path: ^1.7.0
  args: ^1.6.0
  #执行shell命令
  process_run: ^0.11.0+2
executables:
  cpub: cpub

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
